<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/munchkinLightBrown</item>
        <item name="colorPrimaryDark">@color/munchkinBrown</item>
        <item name="colorAccent">@color/munchkinYellow</item>
    </style>

    <style name="BrownEditText">
        <item name="colorControlNormal">@color/munchkinBrown</item>
        <item name="colorControlActivated">@color/munchkinLightBrown</item>
    </style>

</resources>
